package com.yeadun.testutil;import com.yeadun.testutil.product.ProductType;import com.yeadun.testutil.excel.ExcelWorker;import com.yeadun.testutil.net.HttpClientFactory;import java.util.ArrayList;import java.util.HashMap;public class TestContext {    // HttpClientBuilder_Name, _HttpClient    private HashMap<String, ArrayList<HttpClientFactory._HttpClient>> clients = new HashMap<>();    // ExcelWorker_Name, ExcelWorker    private HashMap<String, ExcelWorker> excelWorkers = new HashMap<>();    private ArrayList<ProductType> products = new ArrayList<>();    /**     * httpfactory client excelWork productType     *     * com.lion.testutil.net: clientName     * com.lion.testutil.excel: sheetName colNums rowNums  (work extends ApiFuntion)     *     * */}